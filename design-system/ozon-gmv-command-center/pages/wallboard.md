# Wallboard override

- The target viewport is 1920×1080 with no document scrolling.
- Keep the header, four KPI cards, synchronized charts, store ranking, and live order feed visible at once.
- Use 44–56px KPI values and no text smaller than 16px.
- The live feed is the only internally scrollable region and is capped at 50 rows.
- Full-screen mode is read-only; configuration stays in the authenticated dashboard and stores pages.

