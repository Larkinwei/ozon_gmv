# First-time setup override

- 使用与登录页一致的双栏结构，左侧说明本地数据、常驻服务与密钥保护，右侧只放管理员创建表单。
- 初始化是一次性操作；明确说明没有默认密码，完成后不能再次运行。
- 密码至少 10 个字符，显示确认密码和就地不一致提示。
- 初始化加载期间禁用主按钮，成功后直接进入 Dashboard。
- 在 1024 以下改为上下布局，在 768 以下减少装饰但保留安全说明。
