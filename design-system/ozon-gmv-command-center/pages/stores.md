# Store management override

- Use a conventional responsive admin layout rather than the fixed wallboard grid.
- Labels remain visible above inputs; validation appears beside the relevant field.
- API keys are write-only and never returned to the browser after save.
- Connection testing is a secondary action; saving is the single primary action.
- Store removal is represented by a reversible enabled/disabled switch. There is no hard-delete action.

